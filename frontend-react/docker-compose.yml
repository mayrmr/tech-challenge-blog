version: "3.9"

services:
  web:
    build: .
    container_name: tech-challenge-web
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0
